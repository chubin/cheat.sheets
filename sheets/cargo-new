# cargo-new
# Create a new Cargo package in a new directory.

# Create a new binary package
cargo new my_project

# Create a new library package
cargo new --lib my_library

# Initialize a new cargo package with a VCS
cargo new my_project --vcs git

# Initialize a new cargo package without a VCS
cargo new my_project --vcs none

# Specify the edition for the new package
cargo new my_project --edition 2021

# Create a new package with a specified author
cargo new my_project --author "Your Name <email@example.com>"
